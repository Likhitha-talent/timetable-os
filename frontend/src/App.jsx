import Dashboard from './pages/Dashboard.jsx'

// App is the top-level component. Later, this is where we'd add
// routing (e.g. a login page) — for now it just shows the Dashboard.
function App() {
  return <Dashboard />
}

export default App
